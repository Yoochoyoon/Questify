# Questify 버그 수정 내역 (index.html)

작성: 김한 · 팀장 승인 후 수정 진행
대상: `index.html` (main 기준), 총 6건 수정
검증: jsdom 시뮬레이션으로 전 항목 재현 → 수정 → 통과 확인 (런타임 에러 0건)

> 이 수정은 **버그 수정 전용**입니다. 세모 토글(폴더 접기/펼치기)은 `feature/triangle-toggle` 브랜치에 따로 있으며, 이 파일과 수정 라인이 겹치지 않아 별도 머지 가능합니다.

---

## ❌ 버그 1 — 캐릭터 삭제 후 메모/타이머 모달 먹통 (높음)

**증상:** 캐릭터를 삭제한 뒤 "메모 작성" 또는 타이머 완료 후 메모를 누르면 창이 안 열림.

**원인:** 메모·퀴즈를 배열 인덱스(`charIdx`)로 연결하는데, 삭제 시 전역 포인터 `targetCharIdxForMemo`·`timerSelectedCharIdx`를 보정하지 않아 범위를 벗어남. 그 상태로 `getCharacters()[idx].name`을 접근하면 `TypeError` 발생 → 핸들러 중단.

**수정:**
- `deleteChar` 삭제 콜백에 전역 포인터 범위 클램프 추가
- 크래시 지점(`btn-open-notepad`, `result-btn-memo`, `editMemo`)에 옵셔널 체이닝 + 폴백 `?.name || '캐릭터'`
- `renameChar`에 캐릭터 존재 확인 early-return 가드

---

## ❌ 버그 2 — 드래그 시 캐릭터가 화면 밖으로 사라짐 (높음)

**증상:** 캐릭터를 가장자리에서 다른 캐릭터 근처에 놓으면 화면 밖으로 사라지고, 재접속해도 복구 안 됨.

**원인:** `endCharDrag`의 충돌 회피 로직이 좌표에 `+50px`씩 더하는데 우측/하단 경계 초과를 막지 않아 좌표가 100%를 넘김. `load()` 마이그레이션도 `<0`·`NaN`만 복구하고 `>100`은 방치.

**수정:**
- `endCharDrag`에서 최종 좌표를 `pctX = Math.max(2, Math.min(92, pctX))`, `pctY = Math.max(5, Math.min(90, pctY))`로 클램프
- `load()` 마이그레이션에 `x/y < 0 || > 100` 복구 조건 추가 (기존 실종된 캐릭터도 재접속 시 화면 안으로 복귀)

---

## ⚠️ 버그 3 — Date.now() ID 충돌 (중간)

**증상:** 퀘스트/하위/메모/퀴즈를 빠르게 연속 생성하면 ID가 겹쳐서, 두 번째 항목을 클릭/삭제해도 첫 번째가 반응.

**원인:** todo·subtask·memo·quiz ID가 전부 `Date.now()`라 같은 밀리초 생성 시 중복. `find(id)`가 첫 번째만 반환.

**수정:**
- 고유 ID 생성기 `uid()` 추가 (`Date.now() * 1000 + 순번`, 숫자 ID 유지 → `parseInt` 호환)
- ID 생성 4곳(todo/subtask/memo/quiz)을 `uid()`로 교체
- ※ 타이머 타임스탬프용 `Date.now()`는 그대로 보존

---

## ⚠️ 버그 4 — 물음표 가이드, 모바일에서 안 열림 (중간)

**증상:** 좌하단 ? 가이드가 데스크탑 hover로만 열려서 모바일에선 탭해도 툴팁이 안 뜸.

**수정:**
- CSS에 `.show-guide` 토글 셀렉터 추가 (hover + tap 둘 다 지원)
- ? 아이콘 탭 시 토글, 외부 탭 시 닫히는 JS 추가

---

## 🟢 버그 5 — gainXP 음수 인덱스 방어 (저위험, 예방 차원)

`gainXP`가 `charIdx >= length`만 막고 음수는 안 막던 것을 `|| charIdx < 0` 추가로 보완.

---

## 검증 결과

| 항목 | 재현 | 수정 후 |
|---|:---:|:---:|
| 버그1 삭제 후 모달 | TypeError 발생 | ✅ 정상 오픈 |
| 버그2 좌표 화면 밖 | x=110% 이탈 | ✅ (50,80) 복구 |
| 버그3 ID 충돌 | 중복 발생 | ✅ 유니크 |
| 버그4 모바일 가이드 | 탭 무반응 | ✅ 탭 토글 |
| 퀘스트 추가/하위/완료/탭 | — | ✅ 회귀 0건 |

전체 런타임 에러 0건. 변경 라인은 의도한 곳만 (diff 확인 완료).
